import type { BookingBusinessSettings } from "@/features/booking/types/business"

const mockBookingBusiness: BookingBusinessSettings = {
  isSoloBusiness: true,
  name: "Mocha Beauty Studio",
}

export { mockBookingBusiness }
